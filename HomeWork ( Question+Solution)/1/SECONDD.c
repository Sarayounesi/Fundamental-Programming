#include <stdio.h>
int main(void)
{
     int tmp;

    scanf("%d",&tmp);
   if(tmp<=0)
    printf("Ice");
   else if(100<=tmp)
      printf("Steam" );
   else 
      printf("Water" ) ;     

}
